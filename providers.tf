provider "aws" {
  region = "us-east-1"
  # Specify additional configuration settings as needed, such as access_key and secret_key
  # access_key = "YOUR_ACCESS_KEY"
  # secret_key = "YOUR_SECRET_KEY"
}
